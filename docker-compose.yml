services:
#  winterflow-api:
#    build:
#      context: .
#      dockerfile: winterflow-api.Dockerfile
#    depends_on:
#      - winterflow-bus
#    env_file:
#      - .env
#    ports:
#      - "8080:8080"

  winterflow-bus:
    image: redis:8-alpine
    command: /bin/sh -c "redis-server --requirepass $$REDIS_PASSWORD"
    env_file:
      - .env
    ports:
      - "6379:6379"
#    volumes:
#      - winterflow-bus-redis:/data

  winterflow-db:
    image: postgres:17
    environment:
      POSTGRES_USER: wfuser
      POSTGRES_PASSWORD: wfpassword
      POSTGRES_DB: winterflow
    ports:
      - "5432:5432"
#    volumes:
#      - winterflow-db-data:/var/lib/postgresql/data

#volumes:
#  winterflow-bus-redis:
